<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          xmlns:tools="http://schemas.android.com/tools">
    <application
        android:allowBackup="true"
        android:icon="@mipmap/appicon"
        android:roundIcon="@mipmap/appicon_round"
        android:supportsRtl="true"
        android:label="Govee Temp Control">

        <!-- La activity principal (con LAUNCHER) la genera el atributo
             [Activity(MainLauncher = true)] de MainActivity.cs. No declararla
             también acá: el nombre de clase autogenerado por .NET Android es
             un hash (crc64...), distinto de ".MainActivity", así que una
             declaración manual queda como un segundo ícono fantasma sin
             clase real detrás. -->

    </application>

    <!-- Permisos requeridos -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.CHANGE_NETWORK_STATE" />
    
    <!-- Para mantener la app activa en background y detectar cambios de conexión -->
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    
    <uses-sdk android:minSdkVersion="21" android:targetSdkVersion="35" />
</manifest>