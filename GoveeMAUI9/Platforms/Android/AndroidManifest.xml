<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          xmlns:tools="http://schemas.android.com/tools">
    <application
        android:allowBackup="true"
        android:icon="@mipmap/appicon"
        android:roundIcon="@mipmap/appicon_round"
        android:supportsRtl="true"
        android:label="Govee Temp Control">

        <!-- Activity principal — única con LAUNCHER -->
        <activity
            android:name="com.personal.goveetempcontrol.MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:theme="@style/Maui.SplashTheme"
            android:configChanges="screenSize|orientation|uiMode|screenLayout|smallestScreenSize|density"
            tools:node="replace">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Evitar que la activity de splash de MAUI aparezca como segundo icono -->
        <activity-alias
            android:name="com.personal.goveetempcontrol.MainActivityAlias"
            android:targetActivity="com.personal.goveetempcontrol.MainActivity"
            android:exported="false"
            tools:node="remove" />

    </application>

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-sdk android:minSdkVersion="21" android:targetSdkVersion="35" />
</manifest>